// TODO: HomePage — dashboard for authenticated users, route: /dashboard
//
// Requirements:
// - Stat cards row (use .stats-grid from analytics.css):
//     Total Events | Upcoming Events (within 30 days) | Total Tasks | Total Budget
// - Recent events table (last 5 events, sorted by date desc)
// - Quick action buttons: "+ New Event" → /events/new, "+ New Task" → /tasks
// - Fetch data: fetchEvents() from eventService, fetchTasks() from taskService
// - Show loading state while fetching, error state if fetch fails
//
// Imports needed:
// import { useEffect, useState } from 'react';
// import { useNavigate } from 'react-router-dom';
// import { fetchEvents } from '../api/eventService';
// import { fetchTasks } from '../api/taskService';
// import type { Event } from '../types/Event';
// import type { Task } from '../types/Task';

function HomePage() {
  return <div>HomePage (Dashboard) — TODO</div>;
}

export default HomePage;
